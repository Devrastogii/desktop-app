{
  "name": "electronjs",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "cd ../Web/frontend && npm run build && cd ../Desktop && rm -rf ./builder && rm -rf ./dist && mkdir -p ./builder && cp -a ../Web/build/. ./builder && electron-builder"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^27.0.3",
    "electron-builder": "^24.6.4"
  },
  "dependencies": {    
    "electron-is-dev": "^2.0.0",
    "electronmon": "^2.0.2"
  }
}
